import 'package:flutter/material.dart';

class AppLightColors {
  static const Color primary = Color(0xFF261FB3);
  static const Color secondary = Color(0xFF161179);
  static const Color scaffold = Color(0xFF0C0950);
  static const Color surface = Colors.white;

  static const Color textTitle = Colors.black;
  static const Color textBody = Color(0xFF666666);
  static const Color textHint = Color(0xFF999999);

  static const Color onPrimary = Colors.white;
  static const Color onSecondary = Colors.white;

  static const Color error = Colors.redAccent;
  static const Color onError = Colors.white;

  static const Color ratingStar = Colors.amber;
}
